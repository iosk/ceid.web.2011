<h1>biMarks, because even the bookmarks can be bi.</h1>
<p>fuck you i won't do what you tell me</p>
revision 1


<!--
begin tag cloud : generated by TagCrowd.com
Feel free to modify as long as you keep this notice.

EMBEDDING INSTRUCTIONS:
1. Customize your cloud's style by editing the CSS where it says CUSTOMIZE below.
2. Insert this code in its entirety into your webpage or blog post.

This code and its rendered image are released under the Creative Commons Attribution-Noncommercial 3.0 Unported License. (http://creativecommons.org/licenses/by-nc/3.0/)

For COMMERCIAL USE LICENSING, visit http://tagcrowd.com/licensing.html
-->

  <div id="tagcloud", class="round">
    <% for tag in @tag_unique  %>
      <span  class="tag<%=weight(tag)%>"><%= link_to tag.name, tag %></a></span>
    <% end %> 
  </div>


  <% if signed_in? %>
	  <p><%= button_to "New bookmark", new_bookmark_path, :id => "open-contacts-dialog-btn", 
	        :class => "inbox-sf-add-btn tip", :method => :get %></p>
  <% end %>
  
  <%= form_tag 'search', :method => 'get', :id => "bookmarks_search" do %>
  <!-- The radio button submits the search type we want to do -->
    <div class="form_row">
    <label for="theme">Search:</label><br />
    <% [ 'tags', 'title' ].each do |search_type| %>
      <%= radio_button_tag 'search_type', search_type, @search_type == search_type %>in
      <%= search_type.humanize %>
    <% end %>
    </div>
 
    <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search" %>
    </p>
    
  <% end %>
<table>
	<caption>Most Recent</caption>
	  <tr>
	  <th>Title</th>
	  <th>url</th>
		<th>Submitted at</th>
		<th>Submitted by</th>
		<th>Rating</th>
	  </tr>
	  <%= render @bookmarks_most_recent %>
</table>

<table>
	<caption>Top Rated</caption>
	  <tr>
	  <th>Title</th>
	  <th>url</th>
		<th>Submitted at</th>
		<th>Submitted by</th>
		<th>Rating</th>
	  </tr>
	  <%= render @bookmarks_top_rated %>
</table>




</center>

